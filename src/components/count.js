import React,{ Component } from "react";
const Count = (props) => {
    return(
        <>
        {props.name} <br/>
        {props.age} <br/>
        
        </>

    )
}
export default Count 
